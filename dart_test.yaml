tags:
  golden:
    skip: "Golden tests compare raster output and are tied to one machine and Flutter version. Run manually: flutter test --tags golden --run-skipped --update-goldens test/walk_golden_test.dart"
